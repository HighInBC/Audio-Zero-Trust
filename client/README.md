# Client

Client-side host tooling and SDK for Audio-Zero-Trust.

- `tools/` contains CLI, SDK, and helper scripts.
- `test/` contains client host-side integration/HIL tests.
- C++ unit tests for shared/firmware libraries live under `../firmware/test/unit/`.
